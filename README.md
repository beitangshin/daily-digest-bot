# Daily Digest Bot

A configuration-driven Python utility that renders a daily Markdown digest from local JSON input. It deliberately has no embedded webhook, credentials, organization name, or external service dependency.

```bash
python -m daily_digest agenda.json --output digest.md
```

Use the generated Markdown with any approved communication channel.
